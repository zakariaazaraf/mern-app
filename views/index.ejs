<h2>Recently added</h2>
<div>
    <% books.forEach(book =>{
        %>  
            <h4><%= book.title %></h4>
            <img height="450" width="350" src="<%= book.coverImagePath %>">
        <%
    })%>
</div>